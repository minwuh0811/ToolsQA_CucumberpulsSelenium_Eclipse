package Test;
import mainKommun.kommunList;
import static org.junit.Assert.assertEquals;
import org.junit.jupiter.api.Test;
import mainKommun.Kommun;


public class addKommunTest{
	
@Test
	public void addKommunPartilleTest() {
		Kommun kommunTest=new Kommun("partille");
		kommunList kommunListTest= new kommunList();
		kommunListTest.addKommun(kommunTest);
		assertEquals(1,kommunListTest.getkommunList().size());		
}
}
